# @(#) $Revision: 4.11 $

Judy_3x.htm	the Judy(3X) overview manual entry in HTML format;
		normally placed where a web browser can read it

Judy1_3x.htm		describes the Judy1*() macros
Judy1_funcs_3x.htm	describes the Judy1*() functions
JudyL_3x.htm		describes the JudyL*() macros
JudyL_funcs_3x.htm	describes the JudyL*() functions
JudySL_3x.htm		describes the JudySL*() macros
JudySL_funcs_3x.htm	describes the JudySL*() functions
JudyHS_3x.htm		describes the JudyHS*() macros
JudyHS_funcs_3x.htm	describes the JudyHS*() functions

# Note:  The library package README file comes from the following file, but the
# hierarchy of example sources package README files come from each
# corresponding directory:

README_deliver	packaged with other Judy delivered files, renamed to just
		"README" in that context
